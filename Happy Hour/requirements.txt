# Happy Hour pipeline dependencies
pymongo>=4.6
requests>=2.28
beautifulsoup4>=4.11
PyMuPDF>=1.23          # 'import fitz' — PDF text extraction

# LLM-driven pass 1 (pass1_llm.py) talks to a backend via llm_client.py.
# DEFAULT backend is LOCAL Ollama — free, private, NO Anthropic tokens.
#   Install once: https://ollama.com   then:  ollama pull llama3.1
#   (no Python package needed — llm_client calls Ollama's HTTP API with `requests`).
#
# Anthropic is OPTIONAL and only used if you set LLM_PROVIDER=anthropic (costs tokens).
# Uncomment if you want that opt-in path:
# anthropic>=0.49

# Playwright renders JS-heavy sites (Squarespace/Wix/Toast) that requests can't see.
# After install:  python -m playwright install chromium
playwright>=1.40

# Optional: only needed if you enable the scikit-learn classifier path in
# drink_normalizer.py (the stdlib normalizer works without these).
# scikit-learn>=1.3
# fuzzywuzzy>=0.18
# python-Levenshtein>=0.21
